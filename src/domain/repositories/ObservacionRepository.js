import { noImplementado } from './_contract.js';

export class ObservacionRepository {
    /** @returns {Promise<void>} */
    crear(nuevaObservacion)     { noImplementado('ObservacionRepository', 'crear'); }
    /** @returns {Promise<Observacion[]>} */
    listarPorHijo(estudianteId) { noImplementado('ObservacionRepository', 'listarPorHijo'); }
}
